<%= form_with(model: storage_key) do |form| %>
  <% if storage_key.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(storage_key.errors.count, "error") %> prohibited this storage_key from being saved:</h2>

      <ul>
        <% storage_key.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.collection_select(:user_id, User.all, :id, :first_and_last_name) %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
