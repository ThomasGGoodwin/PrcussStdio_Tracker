<p id="notice"><%= notice %></p>

<h1>Users</h1>

<table>
  <thead>
    <tr>
      <th>User ID</th>
      <th>First name</th>
      <th>Last name</th>
      <th>Email</th>
      <th>Role</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @users.each do |user| %>
      <tr>
        <td><%= user.id %></td>
        <td><%= user.first_name %></td>
        <td><%= user.last_name %></td>
        <td><%= user.email %></td>
<<<<<<< HEAD
        <td><%= Role.where(id: user.role).last.role_description %></td>
=======
        <td><%= user.role_id %></td>
>>>>>>> test
        <td><%= link_to 'Show', user %></td>
        <td><%= link_to 'Edit', edit_user_path(user) %></td>
        <td><%= link_to 'Remove', user, method: :delete, data: { confirm: 'Are you sure you want to remove ' + user.first_name + ' ' + user.last_name + ' from the organization?'} %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Generate Attendance Report', attendance_report_path %>
<br>
<%= link_to 'New User', new_user_path %>
<br>
<%= link_to 'Back', root_url %>
