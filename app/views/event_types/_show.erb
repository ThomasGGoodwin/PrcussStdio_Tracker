<%= render 'shared_partials/admin_nav' %>

<div class="jumbotron" style="background-color:white">
<p id="notice"><%= notice %></p>

<p>
  <strong>Event type ID:</strong>
  <%= @event_type.id %>
</p>

<p>
  <strong>Description:</strong>
  <%= @event_type.description %>
</p>

<%= link_to 'Edit', edit_event_type_path(@event_type), class:"btn btn-secondary a-primary" %>
<%= link_to 'Back', event_types_path, class:"btn btn-secondary a-primary" %>
</div>